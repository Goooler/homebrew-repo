{
  "$schema": "https://docs.renovatebot.com/renovate-schema.json",
  "extends": [
    "config:recommended"
  ],
  "packageRules": [
    {
      // We use `brew bump` instead.
      "enabled": false,
      "managerFilePatterns": [
        "^Formula/[^/]+[.]rb$"
      ]
    }
  ]
}
