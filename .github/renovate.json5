{
  "$schema": "https://docs.renovatebot.com/renovate-schema.json",
  "extends": [
    "config:recommended",
  ],
  "packageRules": [
    {
      // We use `brew bump` instead.
      "matchManagers": ["homebrew"],
      enabled: false,
    },
  ],
}
